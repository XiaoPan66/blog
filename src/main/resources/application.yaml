spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://120.76.119.66:3306/blog?allowMultiQueries=true
    username: ply
    password: q123456

  profiles:
    active: proc

  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

